gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: the pinned debug keystore is committed so every clone signs
# identically and `flutter run` is always an upgrade install. Release
# keystores must never be committed.
!app/debug.keystore
